# ============================================================================
# BEDTIME SOLVED - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================================================

# Supabase (get from Supabase Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Service Role Key (keep secret! Used for server-side operations)
# Get from Supabase Dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# OpenRouter API Key (get from https://openrouter.ai/keys)
OPENROUTER_API_KEY="sk-or-v1-..."

# Admin API Key (generate a secure random string)
# Used to authenticate calls to /api/admin/* endpoints
# Generate with: openssl rand -hex 32
ADMIN_API_KEY="your-admin-api-key"

# Optional: Vercel OIDC Token (auto-provided in Vercel deployments)
# VERCEL_OIDC_TOKEN=""
